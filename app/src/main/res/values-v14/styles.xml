<?xml version="1.0" encoding="utf-8"?>
<resources>


    <!-- ActionBar Style -->
    <style name="ActionBar" parent="Widget.AppCompat.ActionBar">
        <item name="android:titleTextStyle">@style/ActionBarTitle</item>
        <item name="android:subtitleTextStyle">@style/ActionBarSubTitle</item>
        <item name="android:background">@drawable/action_bar</item>
        <item name="android:displayOptions">showTitle</item>
    </style>

    <!-- Theme -->
    <style name="Theme.SpecialWhite" parent="Theme.AppCompat.Light.DarkActionBar">
        <item name="android:windowBackground">@color/background_normal</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:actionBarStyle">@style/ActionBar</item>
        <item name="searchViewCloseIcon">@drawable/menu_icon_close</item>
        <item name="searchViewGoIcon">@drawable/menu_icon_search</item>
        <item name="searchViewSearchIcon">@drawable/menu_icon_search</item>
    </style>

</resources>